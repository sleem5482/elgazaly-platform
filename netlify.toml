[build]
  command = "npm run build"
  publish = "dist"

# Proxy API requests through Netlify function to avoid CORS
# Using rewrite to pass POST body correctly
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api-proxy"
  status = 200
  force = true

